repos:
  - repo: local
    hooks:
      - id: encrypt-sops-secrets
        name: Encrypt SOPS secrets before push
        entry: bash scripts/encrypt-secrets.sh
        language: system
        always_run: true
        pass_filenames: false
        stages: [push]
