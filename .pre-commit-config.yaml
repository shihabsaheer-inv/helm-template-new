repos:
  - repo: local
    hooks:
      - id: decrypt-sops-secrets
        name: Decrypt SOPS secrets before deploy
        entry: bash scripts/decrypt-secrets.sh
        language: system
        always_run: true
        pass_filenames: false
